#!/usr/bin/env bash
# Mock fzf for testing
# Returns the value set in FZF_MOCK_SELECTION, or exits 1 if empty

if [ -n "${FZF_MOCK_SELECTION:-}" ]; then
  echo "$FZF_MOCK_SELECTION"
else
  # Output nothing, exit 0 so set -e doesn't trap before the empty check
  echo -n ""
  exit 0
fi
